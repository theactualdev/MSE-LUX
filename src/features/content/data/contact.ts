import { siteConfig } from '@/lib/config'

export const CONTACT_INFO = {
  // The store's domain is mselux.CO. This address is published on the contact
  // page and must be a mailbox that actually receives — it is the only channel
  // the returns policy tells customers to use.
  email: 'hello@mselux.co',
  instagram: siteConfig.social.instagram,
  location: 'Lagos, Nigeria',
  hours: 'Mon – Sat, 9am – 6pm WAT',
  responseTime: 'We reply to emails and DMs within 1–2 business days.',
}
